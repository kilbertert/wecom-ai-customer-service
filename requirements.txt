# Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 数据验证和配置
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
PyYAML>=6,<7
httpx==0.28.0
aiofiles>=24.1.0

# Bug 反馈关系型事实源 (PostgreSQL 生产 / SQLite 测试)
SQLAlchemy>=2.0,<3
asyncpg>=0.29,<1
aiosqlite>=0.20,<1
alembic>=1.13,<2

# 加密和安全
cryptography==41.0.7
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 异步任务处理 (可选)
celery==5.3.4
flower==2.0.1

# Redis (celery broker + 持久消息队列/分布式锁/RedisDedupStore; redis.asyncio 自 4.2 起内置)
redis>=5.0,<6

# 监控和日志
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.38.0

# 测试框架
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# 媒体处理
pydub==0.25.1
wechatpy==1.8.0

# 语音识别 (ASR) - wecom 侧 paraformer 转写语音为文本入 query (Dify 无 ASR 节点)
dashscope>=1.26.0

# 代码质量工具
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1
pre-commit==3.5.0

pycryptodome>=3.19.0
psutil>=5.9.0
